#!/bin/sh
# Root-owned publisher. Exports only bounded metadata and opaque backup IDs;
# it never copies, moves, deletes, starts, or stops any world or service.
set -eu

exec /usr/bin/python3 /opt/valheim/modpack/lib/world_backups.py inventory
